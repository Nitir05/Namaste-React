01. What is Emmet?
Ans:- Emmet is an free add-on for our editor that takes shortcut from user and return the full piece of code.

02. Difference between a library and a framework?
Ans:- Framework provides complete set of tools to develop a production ready application where as a libraary targets the specific
      functionality of an application, library can co exists with different libraries where as framework can't.

03. What is CDN? why do we use it?
Ans: CDN stands for content delievery network, CDN are the servers which stores the resources, whenever user access the CDN links they 
     get the resource from those server which are gegraphically closer to them.

04. Why is React known as React?
Ans:- React is called React because of its core features which enables it to react to the data change dynamically.

05. What is cross-origin in the script tag?
Ans:- Cross-origin in the script tag tells that the resource is being access from some other resource.

06. What is Difference between React and ReactDOM?
Ans:- React is a library which is used to build User interfaces where as ReactDOM is an complimentary library that glues React and Browser DOM.

07: What is difference between react.development.js and react.production.js CDN links?
Ans:- react.development.js includes the react resource which are useful for development where as production files are minified and optimized links for Production apps.